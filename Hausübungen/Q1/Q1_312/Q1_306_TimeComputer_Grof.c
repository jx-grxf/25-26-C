#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main () {

  int seconds = 0;
  int minutes = 0;
  int hours = 0;
  int Time = 0;

  srand(time(NULL));

  Time = rand();

  printf("Input the number of seconds since midnight: %d", seconds);

  return 0;





}